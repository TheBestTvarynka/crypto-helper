
# prop-strategies

This crate contains [`prop`](https://docs.rs/proptest/latest/proptest/) strategies for the [`asn1-parser`](../asn1-parser/).

TODO: need to fix all prop tests because the API has changed recently.